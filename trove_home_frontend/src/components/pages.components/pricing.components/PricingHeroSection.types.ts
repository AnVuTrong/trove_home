export interface PricingHeroSectionProps {
  className?: string;
  titleKey?: string;
  descriptionKey?: string;
}

export interface PricingHeroSectionStyles {
  sectionContainer: string;
  contentContainer: string;
  title: string;
  description: string;
} 